

#### Value types

The following describes the types that can be defined when using the [create](((security-guide))/create-{props.endpoint_artifact_api_doc_name}-item-api.html) or [update](((security-guide))/update-{props.endpoint_artifact_api_doc_name}-item-api.html) {props.endpoint_artifact_name} APIs:

<div id="exception-{props.endpoint_artifact_api_doc_name}-item-comment-schema"></div>

##### `comment` object schema

Comments are JSON objects containing the following structure:

```json
{
    "comment": "some comment here"
}
```

When used with the update API, existing comments can be updated by using their associated `id`, while any comment without the `id` attribute will be added as a new comment:

```json
{
    "comment": "some comment here - updated",
    "id": "1078cf59-5893-4143-acf7-40a40af16bee"
}
```

<div id="exception-{props.endpoint_artifact_api_doc_name}-item-os-types"></div>

##### `os_types` values

The following are the valid OS types that can be used when creating {props.endpoint_artifact_name}:

* `windows`
* `linux`
* `macos`

<div id="exception-{props.endpoint_artifact_api_doc_name}-item-scope-assignment-tags"></div>

##### Scope assignment

{props.endpoint_artifact_name_sentence_start} can be assigned globally across all endpoint policies, or assigned to specific policies. You can assign the {props.endpoint_artifact_name_single} by defining one or more tags with a prefix of `policy:`. Note that the {props.endpoint_artifact_name_single} can be either global or per policy, but not **both**. The following tags are available for use in order to control the assignment scope:

* `policy:all` : {props.endpoint_artifact_name_single_sentence_start} is global to all policies. If used, no other `policy:` tag is allowed.
* `policy:<endpoint-policy-id>` : {props.endpoint_artifact_name_single_sentence_start} is assigned to a policy. Multiple `per policy` tags can be used to associate the {props.endpoint_artifact_name_single} to multiple policies.

