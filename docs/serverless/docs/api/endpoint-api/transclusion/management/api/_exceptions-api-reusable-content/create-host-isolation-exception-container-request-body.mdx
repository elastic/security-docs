

| Name | Type | Description | Required |
|---|---|---|---|
| `description` | String | Describes the {props.endpoint_artifact_name} container. | Yes |
| `list_id` | String | Must be set to `pass:a[{props.endpoint_artifact_list_id}]`. | Yes |
| `meta` | Object | Placeholder for metadata about the list container. | No |
| `name` | String | The {props.endpoint_artifact_name} container's name. | Yes |
| `namespace_type` | String | Must be set to `agnostic`. | Yes |
| `tags` | String[] | String array containing words and phrases to help categorize the {props.endpoint_artifact_name} container. | No |
| `type` | String | Must be set to `endpoint_host_isolation_exceptions`. | Yes |