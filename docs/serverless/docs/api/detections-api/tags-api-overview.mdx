---
id: serverlessSecurityTagsApiOverview
# slug: /serverless/security/tags-api-overview
title: Tags endpoint
# description: Description to be written
tags: [ 'serverless', 'security', 'reference' ]
status: rough content
---

<div id="tags-api-overview"></div>

Aggregates and returns all rule tags.

## Get tags

Aggregates and returns all unique tags from all rules.

### Request URL

`GET <kibana host>:<port>/api/detection_engine/tags`

#### Example request

Gets tags for all rules in the ((kib)) default space:

```console
GET api/detection_engine/tags
```
{/* KIBANA */}

### Response code

`200` 
    : Indicates a successful call.

#### Example response

```json
[
  "zeek",
  "suricata",
  "windows",
  "linux",
  "network",
  "initial access",
  "remote access",
  "phishing"
]
```
